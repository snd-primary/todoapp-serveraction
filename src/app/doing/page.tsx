import Timer from "@/components/Timer";
import { totalSeconds } from "@/lib/totalSeconds";

const TodoTimer: React.FC = () => {
  return <>{/* <Timer title={data.title} initialCount={TOTALSEC} /> */}</>;
};

export default TodoTimer;
